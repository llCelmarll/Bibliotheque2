# Dockerfile pour Frontend React Native/Expo Web - DÉVELOPPEMENT
FROM node:22-alpine

WORKDIR /app

# Installer les dépendances système nécessaires
RUN apk add --no-cache git

# Copier package.json et installer les dépendances
COPY package*.json ./
RUN npm ci

# Exposer le port Expo Web
EXPOSE 8081

# Démarrer en mode développement
CMD ["npm", "run", "web"]
